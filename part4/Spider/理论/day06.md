```
代理ip，去各大网站西刺代理
爬下来后，text，json格式存取，数据库也可以存
我们的存取到时要拿出来用，现在就是选择数据库
mysql关系型的，重点是表和表之间建立关系，查的话是比较慢的，有的很久出不来结果，sql的话一千都压力很大，所有我们选择的是redis,代理ip肯定选择集合，而且是有序集合
还可以排序，好用的就放在前面，不好的就放在后面，下次使用过的时候就比较快了。
最后是检测模块，检测不好用，就减分，分太低我就不要了。
检测的方法，就是用要去爬的那个网站，爬谁就检测谁，接下来接口模块，就是怎么拿给别人用，做一个接口，接口一访问就行了，基本上这4大模块基本是必考的。
实际上，代理池的维护是很简单的

run 里面就是初始化
首先看init，证明是用的构造的默认的对象
然后看怎么调的，run 方法
要找在哪里配的，各种参数

redis_key=""这里是可以改的，即使没有，也会自动放进去

```

```

```